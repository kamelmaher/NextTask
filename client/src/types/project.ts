export type Project = {
    id: string;
    title: string;
    description: string;
    minPrice: number;
    maxPrice: number;
    category: string;
    deliveryDuration: number;
    createdAt: string;
    userId: string;
}